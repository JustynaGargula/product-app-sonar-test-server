run:
  timeout: 2m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gofmt
    - goimports
    - revive
    - unused
    - gosimple
    - gocritic
    - gosec

issues:
  exclude-use-default: false
